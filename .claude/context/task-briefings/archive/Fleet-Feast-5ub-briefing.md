# Task Briefing: Fleet-Feast-5ub

**Generated**: 2025-12-05T03:00:00Z
**Agent**: Casey_Components
**Task**: Navigation & Layout System
**Invocation**: 1

---

## Objective

Create a responsive navigation system and page layout components including mobile hamburger menu, desktop sidebar/header, page layouts, header/footer components, breadcrumbs, and role-based navigation items.

## Acceptance Criteria

1. **Main Navigation Component**
   - Desktop: Horizontal header navigation OR sidebar (design choice)
   - Mobile: Hamburger menu with slide-out drawer
   - Role-based menu items (Customer, Vendor, Admin see different items)
   - Active state highlighting
   - User avatar/dropdown for logged-in users

2. **Page Layouts**
   - `MainLayout` - Default layout with nav, footer
   - `DashboardLayout` - For authenticated dashboard pages
   - `AuthLayout` - Clean layout for login/register pages
   - `AdminLayout` - Admin-specific layout with admin nav

3. **Header Component**
   - Logo linking to home
   - Navigation links
   - Search bar (placeholder)
   - User menu (login/register or avatar dropdown)
   - Mobile hamburger trigger

4. **Footer Component**
   - Company info
   - Quick links (About, FAQ, Contact, Terms, Privacy)
   - Social media links (placeholder)
   - Copyright notice

5. **Breadcrumbs Component**
   - Dynamic breadcrumb generation
   - Clickable path items
   - Current page not clickable

## Technical Details

### File Structure
```
components/layout/
├── MainLayout.tsx
├── DashboardLayout.tsx
├── AuthLayout.tsx
├── AdminLayout.tsx
├── Header.tsx
├── Footer.tsx
├── Sidebar.tsx
├── MobileNav.tsx
└── Breadcrumbs.tsx

components/navigation/
├── NavLink.tsx
├── NavMenu.tsx
├── UserMenu.tsx
└── MobileDrawer.tsx
```

### Navigation Items by Role
```typescript
const customerNav = [
  { label: "Search Trucks", href: "/search" },
  { label: "My Bookings", href: "/dashboard/bookings" },
  { label: "Messages", href: "/dashboard/messages" },
  { label: "Favorites", href: "/dashboard/favorites" },
];

const vendorNav = [
  { label: "Dashboard", href: "/vendor/dashboard" },
  { label: "Bookings", href: "/vendor/bookings" },
  { label: "Calendar", href: "/vendor/calendar" },
  { label: "Messages", href: "/vendor/messages" },
  { label: "Profile", href: "/vendor/profile" },
];

const adminNav = [
  { label: "Dashboard", href: "/admin" },
  { label: "Pending Vendors", href: "/admin/vendors/pending" },
  { label: "Disputes", href: "/admin/disputes" },
  { label: "Users", href: "/admin/users" },
  { label: "Analytics", href: "/admin/analytics" },
];
```

### Responsive Breakpoints
From PRD Design Requirements:
- Mobile: < 640px (hamburger menu)
- Tablet: 640px - 1024px (condensed nav)
- Desktop: > 1024px (full nav)

### Using UI Components
Import from the existing component library (Fleet-Feast-bxt):
```typescript
import { Button } from "@/components/ui/Button";
import { Avatar } from "@/components/ui/Avatar";
import { Dropdown } from "@/components/ui/Dropdown";
```

### Color Palette (from PRD)
- Primary: #B91C1C (red-700)
- Primary Hover: #991B1B (red-800)
- Text Primary: #111827 (gray-900)
- Text Secondary: #6B7280 (gray-500)
- Border: #E5E7EB (gray-200)
- Background: #F9FAFB (gray-50)

## Dependencies (Completed)

- Fleet-Feast-bxt: UI Component Library (Button, Avatar, Dropdown, etc.)
- Fleet-Feast-igb: Authentication system (for user role)

## PRD Reference

- **Design Requirements**: Responsive breakpoints, color palette
- **F17**: Booking Dashboard (layout requirements)

## Gap Checklist

After completing the task, verify:
- [ ] Navigation is responsive (mobile hamburger, desktop full)
- [ ] Role-based menu items display correctly
- [ ] Active navigation state is highlighted
- [ ] User menu shows login/register for guests
- [ ] User menu shows avatar/dropdown for logged-in users
- [ ] Layouts render children correctly
- [ ] Footer has all required links
- [ ] Breadcrumbs generate dynamically
- [ ] All components use design system colors
- [ ] Keyboard navigation works (a11y)

---

*Briefing generated by MASTER Orchestrator*
