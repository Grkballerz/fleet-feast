# Task Briefing: Fleet-Feast-4h6

**Generated**: 2025-12-05T04:15:00Z
**Agent**: Ellis_Endpoints
**Task**: Quote Request System
**Invocation**: 1

---

## Objective

Implement quote request system for large/complex events: multi-truck RFQ, custom quote submission, vendor response, and quote comparison.

## Acceptance Criteria

1. **Quote Request Creation**
   - `POST /api/quotes/request` - Create RFQ
   - Event details (date, guests, location)
   - Can select multiple vendors
   - Custom requirements field

2. **Vendor Quote Submission**
   - `POST /api/quotes/:requestId/submit` - Submit quote
   - Pricing breakdown
   - Availability confirmation
   - Custom terms/conditions

3. **Quote Management**
   - `GET /api/quotes/requests` - List user's RFQs
   - `GET /api/quotes/requests/:id` - RFQ detail with quotes
   - `GET /api/quotes/vendor` - Vendor's received RFQs
   - `PUT /api/quotes/:id/accept` - Accept a quote

4. **Quote Comparison**
   - Compare multiple quotes side-by-side
   - Price, inclusions, terms
   - Vendor ratings included

## Technical Details

### File Structure
```
modules/quote/
├── quote.service.ts
├── quote.validation.ts
├── quote.types.ts

app/api/quotes/
├── route.ts              # GET list
├── request/route.ts      # POST create RFQ
├── requests/
│   ├── route.ts          # GET user's RFQs
│   └── [id]/route.ts     # GET detail
├── vendor/route.ts       # GET vendor's RFQs
├── [requestId]/
│   └── submit/route.ts   # POST submit quote
└── [id]/
    ├── route.ts          # GET, PUT
    └── accept/route.ts   # PUT accept
```

### Quote Request Schema
```typescript
interface QuoteRequest {
  id: string;
  customerId: string;
  eventDate: Date;
  eventType: EventType;
  guestCount: number;
  location: string;
  requirements: string;
  budget?: number;
  vendorIds: string[];    // Selected vendors to request from
  status: 'OPEN' | 'CLOSED' | 'ACCEPTED';
  expiresAt: Date;        // Quotes due by
  createdAt: Date;
}

interface Quote {
  id: string;
  requestId: string;
  vendorId: string;
  pricing: {
    basePrice: number;
    perPersonPrice?: number;
    additionalFees?: Array<{ name: string; amount: number }>;
    total: number;
  };
  inclusions: string[];
  terms?: string;
  validUntil: Date;
  status: 'SUBMITTED' | 'ACCEPTED' | 'REJECTED' | 'EXPIRED';
  createdAt: Date;
}
```

### Service Implementation
```typescript
async function createQuoteRequest(data: CreateQuoteRequestInput) {
  const request = await prisma.quoteRequest.create({
    data: {
      customerId: data.customerId,
      eventDate: data.eventDate,
      eventType: data.eventType,
      guestCount: data.guestCount,
      location: data.location,
      requirements: data.requirements,
      budget: data.budget,
      expiresAt: addDays(new Date(), 7),
      status: 'OPEN',
    },
  });

  // Create pending quotes for selected vendors
  await prisma.quote.createMany({
    data: data.vendorIds.map(vendorId => ({
      requestId: request.id,
      vendorId,
      status: 'PENDING',
    })),
  });

  // Notify vendors
  for (const vendorId of data.vendorIds) {
    await notifyVendorOfRFQ(vendorId, request);
  }

  return request;
}
```

## Dependencies (Completed)

- Fleet-Feast-w6w: Food Truck Profiles & Search API ✓
- Fleet-Feast-wu8: Booking System API ✓

## PRD Reference

- **F21**: Quote Request System

## Gap Checklist

- [ ] RFQ creates pending quotes for vendors
- [ ] Vendors can submit quotes
- [ ] Customer sees all quotes
- [ ] Quote comparison available
- [ ] Accept quote creates booking
- [ ] Expired quotes handled
- [ ] Notifications sent

---

*Briefing generated by MASTER Orchestrator*
