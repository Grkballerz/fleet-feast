# Task Briefing: Fleet-Feast-fmh

**Generated**: 2025-12-05T04:30:00Z
**Agent**: Drew_Docs
**Task**: API Documentation
**Invocation**: 1

---

## Objective

Create comprehensive API documentation including endpoint reference, request/response examples, authentication guide, error codes, and rate limits. Generate OpenAPI/Swagger spec.

## Deliverables

### 1. OpenAPI Specification (`docs/openapi.yaml`)

```yaml
openapi: 3.0.0
info:
  title: Fleet Feast API
  version: 1.0.0
  description: Food Truck Marketplace API

servers:
  - url: https://api.fleetfeast.com/v1
    description: Production
  - url: http://localhost:3000/api
    description: Development

paths:
  /auth/register:
    post:
      summary: Register new user
      tags: [Authentication]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        201:
          description: User created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        400:
          $ref: '#/components/responses/ValidationError'
```

### 2. Developer Guide (`docs/API_Guide.md`)

Structure:
- Getting Started
  - Authentication (JWT)
  - Rate Limits
  - Error Handling
- Core Concepts
  - Users & Roles
  - Booking Flow
  - Payment Flow
- API Reference (by domain)
  - Authentication
  - Vendors
  - Trucks
  - Bookings
  - Payments
  - Messages
  - Reviews
  - Notifications

### 3. Error Code Reference

```markdown
| Code | HTTP | Description |
|------|------|-------------|
| AUTH_001 | 401 | Invalid credentials |
| AUTH_002 | 401 | Token expired |
| AUTH_003 | 403 | Insufficient permissions |
| BOOK_001 | 400 | Invalid event date |
| BOOK_002 | 400 | Vendor unavailable |
| BOOK_003 | 400 | Invalid status transition |
| PAY_001 | 400 | Payment failed |
| PAY_002 | 400 | Refund not eligible |
```

### 4. Rate Limit Documentation

```markdown
## Rate Limits

| Endpoint Group | Limit | Window |
|---------------|-------|--------|
| Authentication | 10 req | 15 min |
| General API | 100 req | 1 min |
| Search | 30 req | 1 min |
| Payments | 20 req | 1 min |
```

## Source Material

Read existing documentation:
- `docs/API_Registry.md` - Current endpoint list
- `modules/*/validation.ts` - Request schemas
- `modules/*/types.ts` - Response types
- `lib/middleware/error-handler.ts` - Error codes

## Documentation Standards

1. Every endpoint must have:
   - Description
   - Request body schema
   - Response schema (success + errors)
   - Example request
   - Example response

2. Use consistent terminology:
   - "booking" not "reservation"
   - "vendor" not "food truck owner"
   - "customer" not "user" (when specific)

3. Include curl examples:
```bash
curl -X POST https://api.fleetfeast.com/api/bookings \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"vendorId": "...", "eventDate": "2025-01-15"}'
```

## Dependencies (Completed)

- Fleet-Feast-zjd: API Design & Specification ✓
- Fleet-Feast-wu8: Booking System API ✓
- Fleet-Feast-5cl: Payment & Escrow System ✓

## Gap Checklist

- [ ] OpenAPI spec covers all endpoints
- [ ] Developer guide complete
- [ ] Error codes documented
- [ ] Rate limits documented
- [ ] Examples for all endpoints
- [ ] Authentication guide clear

---

*Briefing generated by MASTER Orchestrator*
