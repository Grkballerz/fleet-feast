# Task Briefing: Fleet-Feast-1ez

**Generated**: 2025-12-05T05:30:00Z
**Agent**: Casey_Components
**Task**: Accessibility Fixes
**Invocation**: 1

---

## Objective

Fix all accessibility issues from the audit. Target: 95%+ WCAG 2.1 AA compliance.

## Critical Fixes (Must Fix)

### 1. Skip Navigation Links

**Issue**: Missing skip navigation for keyboard users

**Fix**: Add to main layout:
```tsx
// components/layout/MainLayout.tsx
<a
  href="#main-content"
  className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:bg-white focus:px-4 focus:py-2 focus:rounded"
>
  Skip to main content
</a>
// ... navigation ...
<main id="main-content" tabIndex={-1}>
  {children}
</main>
```

### 2. Interactive Cards Keyboard Handlers

**Location**: `components/ui/Card.tsx`

**Fix**: Add keyboard support for clickable cards:
```tsx
<div
  role="button"
  tabIndex={0}
  onClick={onClick}
  onKeyDown={(e) => {
    if (e.key === 'Enter' || e.key === ' ') {
      e.preventDefault();
      onClick?.(e);
    }
  }}
>
```

### 3. MobileNav Link Fix

**Location**: `components/navigation/MobileNav.tsx` or similar

**Fix**: Replace `<a>` with Next.js `<Link>`:
```tsx
// Before
<a href="/dashboard">Dashboard</a>

// After
import Link from 'next/link';
<Link href="/dashboard">Dashboard</Link>
```

## High Priority Fixes

### 4. Color Contrast - text-secondary

**Issue**: `text-secondary` fails contrast on gray backgrounds (4.31:1, needs 4.5:1)

**Fix**: Darken text-secondary in Tailwind config:
```js
// tailwind.config.js
colors: {
  secondary: {
    // Increase contrast
    DEFAULT: '#525252', // was #6b7280
  }
}
```

### 5. Dropdown Arrow Key Navigation

**Location**: `components/ui/Dropdown.tsx` or Select components

**Fix**: Add arrow key support:
```tsx
onKeyDown={(e) => {
  if (e.key === 'ArrowDown') {
    e.preventDefault();
    focusNextOption();
  } else if (e.key === 'ArrowUp') {
    e.preventDefault();
    focusPreviousOption();
  }
}}
```

### 6. Search Results Announcements

**Fix**: Add live region for search results:
```tsx
<div aria-live="polite" aria-atomic="true" className="sr-only">
  {searchResults.length} results found
</div>
```

### 7. View Toggle ARIA

**Fix**: Add proper ARIA to toggle buttons:
```tsx
<button
  aria-pressed={view === 'grid'}
  aria-label="Grid view"
>
```

### 8. Navigation Landmarks

**Fix**: Add proper landmarks:
```tsx
<nav aria-label="Main navigation">
<nav aria-label="User menu">
<aside aria-label="Filters">
```

## Source Material

- `docs/Accessibility_Audit_Report.md` - Full audit with all issues

## Deliverables

1. Fix all 3 critical issues
2. Fix all 8 high priority issues
3. Address medium priority issues as time allows
4. Verify with accessibility tools

## Verification

```bash
# Run accessibility tests
npm test -- --testPathPattern=accessibility

# Manual testing
# - Tab through all pages
# - Test with screen reader
# - Check color contrast
```

## Dependencies (Completed)

- Fleet-Feast-8pt: Accessibility Audit ✓

## Gap Checklist

- [ ] Skip navigation added
- [ ] Cards keyboard accessible
- [ ] MobileNav links fixed
- [ ] Color contrast passes
- [ ] Dropdowns keyboard accessible
- [ ] Live regions for updates
- [ ] All landmarks labeled

---

*Briefing generated by MASTER Orchestrator*
