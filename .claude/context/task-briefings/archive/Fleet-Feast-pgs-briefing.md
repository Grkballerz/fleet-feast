# Task Briefing: Fleet-Feast-pgs

**Generated**: 2025-12-05T04:15:00Z
**Agent**: Parker_Pages
**Task**: Customer Dashboard Pages
**Invocation**: 1

---

## Objective

Create the customer dashboard with upcoming/past bookings, messages inbox preview, favorites, review submissions, payment history, and account settings.

## Acceptance Criteria

1. **Dashboard Overview** (`/dashboard`)
   - Upcoming bookings summary (next 3)
   - Recent messages preview
   - Action items (pending reviews, confirmations)
   - Quick stats (total bookings, favorites)

2. **Bookings Section**
   - Link to /bookings (already built)
   - Upcoming events highlight

3. **Messages Section**
   - Link to /messages (already built)
   - Unread count badge

4. **Favorites Section** (`/dashboard/favorites`)
   - List of favorited food trucks
   - Quick book button
   - Remove favorite

5. **Reviews Section** (`/dashboard/reviews`)
   - List of user's submitted reviews
   - Pending reviews (completed bookings without review)
   - Submit review modal

6. **Payment History** (`/dashboard/payments`)
   - List of all payments
   - Status (completed, refunded, pending)
   - Receipt download link

7. **Account Settings** (`/dashboard/settings`)
   - Profile information edit
   - Email preferences
   - Password change
   - Delete account option

## Technical Details

### File Structure
```
app/(customer)/dashboard/
├── page.tsx           # Overview
├── layout.tsx         # Dashboard sidebar layout
├── favorites/page.tsx
├── reviews/page.tsx
├── payments/page.tsx
└── settings/page.tsx
```

### Dashboard Overview
```typescript
function DashboardOverview() {
  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <UpcomingBookingsCard />
      <MessagesPreviewCard />
      <ActionItemsCard />
      <QuickStatsCard />
    </div>
  );
}
```

### Dashboard Layout with Sidebar
```typescript
const DASHBOARD_NAV = [
  { label: 'Overview', href: '/dashboard', icon: Home },
  { label: 'Bookings', href: '/bookings', icon: Calendar },
  { label: 'Messages', href: '/messages', icon: MessageSquare },
  { label: 'Favorites', href: '/dashboard/favorites', icon: Heart },
  { label: 'Reviews', href: '/dashboard/reviews', icon: Star },
  { label: 'Payments', href: '/dashboard/payments', icon: CreditCard },
  { label: 'Settings', href: '/dashboard/settings', icon: Settings },
];
```

Use existing components from components/ui/. Integrate with existing APIs.

## Dependencies (Completed)

- Fleet-Feast-wu8: Booking System API ✓
- Fleet-Feast-2f0: Messaging System ✓
- Fleet-Feast-bxt: UI Component Library ✓
- Fleet-Feast-5ub: Navigation & Layout System ✓

## Gap Checklist

- [ ] Dashboard shows upcoming bookings
- [ ] Messages preview with unread count
- [ ] Favorites list with remove option
- [ ] Reviews section shows pending reviews
- [ ] Payment history with download
- [ ] Settings allow profile edit
- [ ] Mobile responsive sidebar

---

*Briefing generated by MASTER Orchestrator*
