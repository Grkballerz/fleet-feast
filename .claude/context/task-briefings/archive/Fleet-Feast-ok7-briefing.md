# Task Briefing: Fleet-Feast-ok7

**Generated**: 2025-12-05T03:00:00Z
**Agent**: Blake_Backend
**Task**: Vendor Application & Onboarding API
**Invocation**: 1

---

## Objective

Implement the vendor application submission API, document upload handling, admin approval workflow, and vendor profile management.

## Acceptance Criteria

1. **Vendor Application Submission API**
   - `POST /api/vendor/apply` - Submit vendor application
   - Collect: business name, cuisine type, description, price range, capacity, service area
   - Validate all required fields
   - Create vendor record with `PENDING` status

2. **Document Upload**
   - `POST /api/vendor/documents` - Upload required documents
   - Support document types: BUSINESS_LICENSE, HEALTH_PERMIT, INSURANCE, TAX_ID, VEHICLE_REGISTRATION
   - Store files in S3/Cloudinary (use environment variable for storage)
   - Link documents to vendor record

3. **Admin Approval Workflow**
   - `GET /api/admin/vendors/pending` - List pending applications
   - `POST /api/admin/vendors/:id/approve` - Approve vendor
   - `POST /api/admin/vendors/:id/reject` - Reject with reason
   - Only ADMIN role can access these endpoints

4. **Vendor Profile Management**
   - `GET /api/vendor/profile` - Get own profile
   - `PUT /api/vendor/profile` - Update profile
   - `GET /api/vendor/:id/public` - Get public vendor profile

## Technical Details

### File Structure
```
modules/vendor/
├── vendor.service.ts      # Business logic
├── vendor.controller.ts   # Route handlers (if using separate layer)
├── vendor.validation.ts   # Zod schemas
└── vendor.types.ts        # TypeScript types

app/api/vendor/
├── apply/route.ts         # POST application
├── documents/route.ts     # POST/GET documents
├── profile/route.ts       # GET/PUT own profile
└── [id]/
    └── public/route.ts    # GET public profile

app/api/admin/vendors/
├── pending/route.ts       # GET pending list
└── [id]/
    ├── approve/route.ts   # POST approve
    └── reject/route.ts    # POST reject
```

### Service Layer Pattern
Follow the pattern established in `modules/auth/auth.service.ts`:
- Separate business logic from routes
- Use Prisma for database operations
- Throw custom errors with codes and status codes
- Use proper TypeScript types

### Document Storage
Use placeholder for now - check if S3/Cloudinary is configured:
```typescript
// lib/storage.ts
export async function uploadFile(file: File, path: string): Promise<string> {
  // TODO: Implement actual S3/Cloudinary upload
  // For now, return a placeholder URL
  return `https://storage.placeholder.com/${path}/${file.name}`;
}
```

## Dependencies (Completed)

- Fleet-Feast-3rw: Database schema with Vendor, VendorDocument models
- Fleet-Feast-igb: Authentication system with role-based access

## PRD Reference

- **F2**: Food Truck Application - self-service vendor application
- **F3**: Admin Approval Dashboard - review and approve vendors

## Gap Checklist

After completing the task, verify:
- [ ] Application submission creates vendor with PENDING status
- [ ] Document upload stores files and links to vendor
- [ ] Admin endpoints are protected (ADMIN role only)
- [ ] Vendor can view and update own profile
- [ ] Public profile endpoint excludes sensitive data
- [ ] All endpoints have proper error handling
- [ ] Input validation using Zod schemas
- [ ] No SQL injection vulnerabilities

---

*Briefing generated by MASTER Orchestrator*
