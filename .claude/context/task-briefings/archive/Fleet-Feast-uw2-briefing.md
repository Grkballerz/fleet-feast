# Task Briefing: Fleet-Feast-uw2

**Generated**: 2025-12-05T05:30:00Z
**Agent**: Peyton_Performance
**Task**: Performance Optimization
**Invocation**: 1

---

## Objective

Optimize performance to meet PRD targets: LCP <2.5s, TTI <3.5s. Focus on database queries, caching, images, and bundle size.

## Performance Targets (PRD)

| Metric | Target | Current |
|--------|--------|---------|
| LCP (Largest Contentful Paint) | <2.5s | TBD |
| TTI (Time to Interactive) | <3.5s | TBD |
| FCP (First Contentful Paint) | <1.8s | TBD |
| CLS (Cumulative Layout Shift) | <0.1 | TBD |
| Bundle Size (JS) | <200KB gzipped | TBD |

## Optimization Areas

### 1. Database Query Optimization

**Review**:
- `modules/trucks/trucks.service.ts` - Search queries
- `modules/booking/booking.service.ts` - Dashboard queries
- Add proper indexes

**Optimizations**:
```typescript
// Add query optimization
const trucks = await prisma.truck.findMany({
  where: { ... },
  select: { // Only needed fields
    id: true,
    name: true,
    rating: true,
  },
  take: 20, // Pagination
});
```

### 2. Caching Strategy

**Implement**:
```typescript
// lib/cache.ts
import { unstable_cache } from 'next/cache';

export const getCachedTrucks = unstable_cache(
  async (filters) => {
    return prisma.truck.findMany({ where: filters });
  },
  ['trucks'],
  { revalidate: 60 } // 1 minute
);
```

**Cache targets**:
- Truck listings (1 min)
- Vendor profiles (5 min)
- Static content (1 hour)

### 3. Image Optimization

**Verify Next.js Image usage**:
```tsx
import Image from 'next/image';

<Image
  src={truck.image}
  alt={truck.name}
  width={400}
  height={300}
  loading="lazy" // or priority for above-fold
  placeholder="blur"
/>
```

### 4. Bundle Size Reduction

**Analyze**:
```bash
npm run build
# Check .next/analyze output
```

**Optimizations**:
- Dynamic imports for heavy components
- Tree shaking verification
- Remove unused dependencies

```tsx
// Dynamic import for heavy components
const MapView = dynamic(() => import('./MapView'), {
  loading: () => <MapSkeleton />,
  ssr: false,
});
```

### 5. Lazy Loading

- Implement infinite scroll for truck listings
- Lazy load images below fold
- Code split routes

### 6. API Response Optimization

- Compress responses (gzip)
- Minimize payload size
- Add appropriate cache headers

## Deliverables

### 1. Performance Report (`docs/Performance_Report.md`)

```markdown
# Performance Optimization Report

## Baseline Metrics
| Metric | Before | After | Target |
|--------|--------|-------|--------|

## Optimizations Applied
1. Database: [details]
2. Caching: [details]
3. Images: [details]
4. Bundle: [details]

## Lighthouse Scores
- Performance: X/100
- Accessibility: X/100
- Best Practices: X/100
- SEO: X/100
```

### 2. Implementation

- Caching layer (`lib/cache.ts`)
- Database indexes (migration if needed)
- Image optimizations
- Bundle analysis

## Dependencies (Completed)

- Fleet-Feast-qjr: QA Validation ✓

## Gap Checklist

- [ ] LCP < 2.5s achieved
- [ ] TTI < 3.5s achieved
- [ ] Database queries optimized
- [ ] Caching implemented
- [ ] Images optimized
- [ ] Bundle size reduced

---

*Briefing generated by MASTER Orchestrator*
