# Task Briefing: Fleet-Feast-6fo

**Generated**: 2025-12-05T05:00:00Z
**Agent**: Quinn_QA
**Task**: E2E Tests - Critical User Flows
**Invocation**: 1

---

## Objective

Create Playwright E2E tests for critical user flows: registration, vendor onboarding, search to booking, payment, messaging, and review submission.

## Test Structure

```
e2e/
├── auth.spec.ts
├── vendor-onboarding.spec.ts
├── search-booking.spec.ts
├── payment.spec.ts
├── messaging.spec.ts
├── review.spec.ts
└── fixtures/
    └── test-data.ts
```

## Critical Flows

### 1. User Registration & Login (`auth.spec.ts`)

```typescript
import { test, expect } from '@playwright/test';

test.describe('Authentication', () => {
  test('should register new customer', async ({ page }) => {
    await page.goto('/register');
    await page.fill('[name="email"]', 'test@example.com');
    await page.fill('[name="password"]', 'SecurePass123!');
    await page.fill('[name="name"]', 'Test User');
    await page.click('[type="submit"]');
    await expect(page).toHaveURL('/dashboard');
  });

  test('should login existing user', async ({ page }) => {
    await page.goto('/login');
    await page.fill('[name="email"]', 'test@example.com');
    await page.fill('[name="password"]', 'SecurePass123!');
    await page.click('[type="submit"]');
    await expect(page).toHaveURL('/dashboard');
  });

  test('should handle password reset', async ({ page }) => {
    // ...
  });
});
```

### 2. Vendor Onboarding (`vendor-onboarding.spec.ts`)

- Register as vendor
- Fill application form
- Upload documents
- Submit for review
- Admin approval (separate test)

### 3. Search to Booking (`search-booking.spec.ts`)

- Search by location
- Apply filters
- View truck details
- Select date
- Submit booking request
- Vendor accepts
- Customer pays

### 4. Payment Flow (`payment.spec.ts`)

```typescript
test('should complete payment', async ({ page }) => {
  // Use Stripe test card
  await page.fill('[name="cardNumber"]', '4242424242424242');
  await page.fill('[name="expiry"]', '12/30');
  await page.fill('[name="cvc"]', '123');
  await page.click('#submit-payment');
  await expect(page.locator('.success-message')).toBeVisible();
});
```

### 5. Messaging (`messaging.spec.ts`)

- Send message from customer
- Receive message as vendor
- Verify anti-circumvention warning
- Mark as read

### 6. Review Submission (`review.spec.ts`)

- Complete booking (mock)
- Navigate to review form
- Submit rating and comment
- Verify review appears

## Playwright Configuration

```typescript
// playwright.config.ts
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
});
```

## Test Data Fixtures

```typescript
// e2e/fixtures/test-data.ts
export const testCustomer = {
  email: 'e2e-customer@test.com',
  password: 'TestPass123!',
  name: 'E2E Customer',
};

export const testVendor = {
  email: 'e2e-vendor@test.com',
  password: 'TestPass123!',
  businessName: 'E2E Food Truck',
};

export const testBooking = {
  eventDate: '2025-02-15',
  guestCount: 50,
  eventType: 'CORPORATE',
};
```

## Deliverables

1. `e2e/` directory with all test files
2. `playwright.config.ts` configuration
3. Test fixtures
4. CI integration in GitHub Actions
5. Test report

## Dependencies (Completed)

- Fleet-Feast-8ve: Authentication Pages ✓
- Fleet-Feast-r28: Booking Flow Pages ✓
- Fleet-Feast-v16: Messaging Interface ✓

## Gap Checklist

- [ ] All critical flows tested
- [ ] Tests run in CI
- [ ] Fixtures properly isolated
- [ ] Screenshots on failure
- [ ] Reports generated
- [ ] Flaky tests addressed

---

*Briefing generated by MASTER Orchestrator*
