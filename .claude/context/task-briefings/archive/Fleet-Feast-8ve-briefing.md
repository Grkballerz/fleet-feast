# Task Briefing: Fleet-Feast-8ve

**Generated**: 2025-12-05T03:15:00Z
**Agent**: Parker_Pages
**Task**: Authentication Pages
**Invocation**: 1

---

## Objective

Create login, register, email verification, and password reset pages with form validation, error states, and redirect flows.

## Acceptance Criteria

1. **Login Page** (`/login`)
   - Email and password fields
   - "Remember me" checkbox
   - Forgot password link
   - Sign up link
   - Error display for invalid credentials
   - Redirect to dashboard after login

2. **Register Page** (`/register`)
   - Email, password, confirm password fields
   - Role selection (Customer or Vendor)
   - Terms of service checkbox
   - Success message after registration
   - Redirect to email verification prompt

3. **Email Verification Page** (`/verify-email`)
   - Token from URL query param
   - Loading state while verifying
   - Success/error message display
   - Login link after verification

4. **Password Reset Flow**
   - `/forgot-password` - Request reset email form
   - `/reset-password` - New password form with token

## Technical Details

### File Structure
```
app/(auth)/
├── layout.tsx            # AuthLayout wrapper
├── login/page.tsx
├── register/page.tsx
├── verify-email/page.tsx
├── forgot-password/page.tsx
└── reset-password/page.tsx
```

### Form Validation
Use Zod + react-hook-form:
```typescript
const loginSchema = z.object({
  email: z.string().email("Invalid email address"),
  password: z.string().min(8, "Password must be at least 8 characters"),
  rememberMe: z.boolean().optional(),
});
```

### Auth Integration
Use NextAuth signIn/signOut:
```typescript
import { signIn } from "next-auth/react";

const handleLogin = async (data: LoginFormData) => {
  const result = await signIn("credentials", {
    email: data.email,
    password: data.password,
    redirect: false,
  });

  if (result?.error) {
    setError("Invalid email or password");
  } else {
    router.push("/dashboard");
  }
};
```

### API Calls
- Register: `POST /api/auth/register`
- Verify email: `POST /api/auth/verify-email`
- Forgot password: `POST /api/auth/reset-password` (request)
- Reset password: `PUT /api/auth/reset-password` (confirm)

## UI Components Available

From components/ui/:
- Button, Input, Card, Alert, Spinner
- Form controls with error states

From components/layout/:
- AuthLayout (clean centered layout)

## Dependencies (Completed)

- Fleet-Feast-igb: Authentication API endpoints
- Fleet-Feast-bxt: UI Component Library
- Fleet-Feast-5ub: AuthLayout component

## PRD Reference

- **F1**: User Registration & Auth
- Design: Inter font, Primary color #B91C1C

## Gap Checklist

After completing the task, verify:
- [ ] Login form validates email and password
- [ ] Registration requires password confirmation match
- [ ] Role selection defaults to Customer
- [ ] Email verification shows loading state
- [ ] Password reset requires valid token
- [ ] Error messages are user-friendly
- [ ] Success redirects work correctly
- [ ] Forms are accessible (labels, focus states)
- [ ] Mobile responsive design

---

*Briefing generated by MASTER Orchestrator*
