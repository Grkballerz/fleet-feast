import React from "react";

export const metadata = {
  title: "Search Food Trucks - Fleet Feast",
  description: "Find the perfect food truck for your event. Search by cuisine, price, capacity, and more.",
};

export default function SearchLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
